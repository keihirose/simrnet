\name{simrnet}
\alias{simrnet}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Conduct Monte Carlo simulations
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
Conduct Monte Carlo simulations and calculate the root mean squared errors (RMSE).
}
\usage{
simrnet(object, num.obs = c(50, 100, 200, 300, 500, 1000), num.test = 1000, 
        times.sim = 100, nfolds.glmnet=10, msg = TRUE)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{object}{
Parameters required for the simulation, generated by \code{genpar} function. 
}
  \item{num.obs}{
Vector of the numbers of observations (training data) in simulations.
}
  \item{num.test}{
The number of observations for test data in simulations.
}
  \item{times.sim}{
The number of iterations for each \code{num.obs} on simulation.
}
  \item{nfolds.glmnet}{
The number of folds in \code{cv.glmnet} function used in order to choose a regularization parameter in the lasso.
}
  \item{msg}{
Logical flag for displaying the message of which Sigma is currently computed.
}
%  \item{\dots}{Other graphical parameters to plot}
}
%\details{
%%  ~~ If necessary, more details than the description above ~~
%}
\value{
  \item{RMSE}{
  The matrix of RMSE obtained by the Monte Carlo simulation. Its row and column are \code{num.obs} and \code{times.sim}, respectively.
  }
}
\references{
Okinaga, Y., Kyogoku, D., Kondo, S., Nagano, J. A. and Hirose, K.  The Accuracy of Lasso Estimation of Scale-Free Network, \emph{manuscript.}
}
\author{
Kei Hirose and Yuichi Okinaga\cr
\email{mail@keihirose.com}
}
%\note{
%%  ~~further notes~~
%}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{\code{simrnet} object.}

\examples{
data(nagano2019)
attach(nagano2019)
rho <- seq(0.1, 0.9, by = 0.1)
pars <- genpar(X,Y,rho)
result <- simrnet(pars,times.sim=5)
plot(result)
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
